项目介绍

1、项目创建

手动执行 go mod init "name"

2、代码管理——本地用git

git:
    初始化  git init
    做第一次提交
    可选的远程版本库

    配置 .gitignore  这个的意思的不提交版本


1）初始化  git init
PS E:\gotest> git init
Initialized empty Git repository in E:/gotest/.git/

2）做第一次提交
现在联系做手动提交，实操的时候按照自己的习惯来

创建全局的 用户名字和 email
git config --global user.name "jeff"
git config --global user.email "xxx"



清除：git rm --cached .   本地所有

git add xxx    添加具体需要上传的文件或文件夹
 
git commit -m "版本"

3）远程版本库
先在远程git中 创建代码库
比如：代码库 cms

默认产生的库版本是 master

再远程合并
命令:
git remote add origin https://github.com/JeffZengkw/cms.git

git branchi -M master

git push -u origin master

最后配置 .gitignore

作用是让git不提交某些文件

.gitignore 文件的位置要放在 项目文件夹的第一级目录

git commit -m "xxx" 提交在本地代码库中的

提交到远程库中：
git push -u origin master

3、目录结构
main.go 文件放在 项目文件夹的 第一级

4、服务启动与Handler管理
服务启动：

r.RUN()        r := gin.Default()

Handler管理

将Handler的代码放到特点目录中
r.GET("/ping", func(c *gin.Context) { //func(){} 这个就是handler函数
		c.JSON(200, gin.H{
			"msg": "pong",
		})
	})
将func(c *gin.Context) {c.JSON(200, gin.H{
			"msg": "pong",
		})
	}
放到特定目录中的.go文件里面  在r.GET("/路由",文件夹.handler函数名)

比如：/handlers/system/
     /handlers/users/
     /handlers/content/

故：将所有handler代码放在特定的 handlers 文件夹中，再根据业务逻辑分类    

r.GET("/ping", system.Ping)   system.Ping函数定义在system业务目录中的.go文件中的Ping()函数

func Ping(ctx *gin.Context) {
	ctx.JSON(200, gin.H{
		"msg": "pong",
	})
}

5、将路由集中管理

思路：
    原则：路由变动，不修改main.go这个文件
    
    路径：将初始化路由引擎的操作单独处理
          再根据业务，将路由分散到具体的业务处理中。业务目录 就是考虑 handlers/子目录
    
    方法：增加文件handlers/init.go 初始化文件用于初始化和handler相关的内容，包括路由：
         
//初始化路由引擎——做了单独的处理

func InitEngine() *gin.Engine {
	r := gin.Default()

	r.GET("/ping", Ping)
	return r
}

import (
	"gotest/project/handlers/system"
)

//现在目录的入口文件 main函数 仅需要调用 InitEngine（）函数 接收返回来的 *gin.Engine 数据
//
func main() {
	r := system.InitEngine()
	r.Run()
}

//根据业务，将路由分散到具体的业务处理中，不用一来就把所有的路由初始化