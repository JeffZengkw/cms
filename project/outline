项目介绍

1、项目创建

手动执行 go mod init "name"

2、代码管理——本地用git

git:
    初始化  git init
    做第一次提交
    可选的远程版本库

    配置 .gitignore  这个的意思的不提交版本


1）初始化  git init
PS E:\gotest> git init
Initialized empty Git repository in E:/gotest/.git/

2）做第一次提交
现在联系做手动提交，实操的时候按照自己的习惯来

创建全局的 用户名字和 email
git config --global user.name "jeff"
git config --global user.email "xxx"



清除：git rm --cached .   本地所有

git add xxx    添加具体需要上传的文件或文件夹
 
git commit -m "版本"

3）远程版本库
先在远程git中 创建代码库
比如：代码库 cms

默认产生的库版本是 master

再远程合并
命令:
git remote add origin https://github.com/JeffZengkw/cms.git

git branchi -M master

git push -u origin master

最后配置 .gitignore

作用是让git不提交某些文件

.gitignore 文件的位置要放在 项目文件夹的第一级目录

git commit -m "xxx" 提交在本地代码库中的

提交到远程库中：
git push -u origin master

3、目录结构
main.go 文件放在 项目文件夹的 第一级

4、服务启动与Handler管理
服务启动：

r.RUN()        r := gin.Default()

Handler管理

将Handler的代码放到特点目录中
r.GET("/ping", func(c *gin.Context) { //func(){} 这个就是handler函数
		c.JSON(200, gin.H{
			"msg": "pong",
		})
	})
将func(c *gin.Context) {c.JSON(200, gin.H{
			"msg": "pong",
		})
	}
放到特定目录中的.go文件里面  在r.GET("/路由",文件夹.handler函数名)

比如：/handlers/system/
     /handlers/users/
     /handlers/content/

故：将所有handler代码放在特定的 handlers 文件夹中，再根据业务逻辑分类    

r.GET("/ping", system.Ping)   system.Ping函数定义在system业务目录中的.go文件中的Ping()函数

func Ping(ctx *gin.Context) {
	ctx.JSON(200, gin.H{
		"msg": "pong",
	})
}

5、将路由集中管理

思路：
    原则：路由变动，不修改main.go这个文件
    
    路径：将初始化路由引擎的操作单独处理
          再根据业务，将路由分散到具体的业务处理中。业务目录 就是考虑 handlers/子目录
    
    方法：增加文件handlers/init.go 初始化文件用于初始化和handler相关的内容，包括路由：
         
//初始化路由引擎——做了单独的处理

func InitEngine() *gin.Engine {
	r := gin.Default()

	r.GET("/ping", Ping)
	return r
}

import (
	"gotest/project/handlers/system"
)

//现在目录的入口文件 main函数 仅需要调用 InitEngine（）函数 接收返回来的 *gin.Engine 数据
//
func main() {
	r := system.InitEngine()
	r.Run()
}

//根据业务，将路由分散到具体的业务处理中，不用一来就把所有的路由初始化

比如：
    在handlers/system/router.go 中完成 system相关的路由的初始化；
    同理，如果content业务中有相关路由，
    就在 handlers/content/router.go中 完成  content相关的路由初始化;


6、管理配置文件

配置文件的核心操作：
    存储配置
    解析配置
    使用配置

1）存储配置：
    通常将配置存储在特定格式的配置文件中，例如：
    json
    yaml
    xml
    ini
以yaml格式为例，存储配置

创建配置文件：
    configs.yaml
内容:
    app:
        addr: ":8080"

configs.yaml 配置文件不是程序源码的一部分，是独立的文件。最终会存在一个程序的执行性程序和配置文件：
    A、程序 server.exe
    B、配置文件 configs.yaml

2）解析配置

核心操作：
    读取文件内容
    解析内容到特定格式

解析的时候，往往需要附加操作：
    不同配置格式的支持
    不同位置的支持
    不同存储方式的支持
    默认值

实操的时候，配置内容还可以来自其他服务，比如 nacos服务

推荐使用viper包来实现配置的解析：https://github.com/spf13/viper  第三方包

viper包的特性：
    默认配置
    从JSON，TOML，YAML属性配置文件读取数据
    实时查看和重新读取配置文件
    从环境变量中读取
    从远程配置系统（如：etcd）读取数据并监听变化
    从命令行参数读取
    从buffer中读取
    设置显示值

按照viper包
go get github.com/spf13/viper

编写viper初始配置函数
viper也是核心工具，集中管理。路径： utils/config.go  

新增目录 utils  用来管理系统中用到的工具
编写解析配置文件的函数：
// 默认配置
func defaultConfig() {
	viper.SetDefault("app.addr", ":8080")
}

//编辑配置工具

func ParseConfig() {
	//默认配置
	defaultConfig() //调用函数完成
	//配置解析参数
	viper.AddConfigPath(".")       //从哪些目录搜索config文件
	viper.SetConfigName("configs") //文件名和后缀分开，这里填文件名，下一个 type填格式
	viper.SetConfigType("yaml")    //配置类型（格式）

	//执行解析，调用viper的ReadInConfig()方法
	if err := viper.ReadInConfig(); err != nil {
		log.Fatal(err)

	}

}

在项目初始化时调用配置解析，在比较靠前的位置处理配置文件的情况
在main.go函数中处理

通常第一步就是处理配置文件的信息
func main() {
	//解析配置
	utils.ParseConfig()

	//初始化路由引擎
	r := handlers.InitEngine()
	r.Run()
}

3）使用配置

   以配置监听端口为例：
   
   main.go
   r.Run()  默认是监听 8080端口
   
   现在去使用配置文件configs.yaml中的端口：r.Run(viper.GetString("app.addr"))

7、应用模式配置

模式分类：——比如 不同模式下面  日志内容 可能不同
    发布模式
    调试模式
    开发模式

针对gin框架来说：它是支持模式的

基于配置，来修改gin的模式： 增加配置项

在configs.yaml中增加一个叫做模式的配置项
app:
    mode: "release"
    addr: "9090"

在utils.config.go中的 默认函数中加入默认 值

接下来是： 设置模式——工具级别

utils/mode.go
//模式设置函数
func SetMode() {
	switch strings.ToLower(viper.GetString("app.mode")) { //strings.ToLower 是为了避免 大小写问题，统一转换为 小写
	case "release":
		gin.SetMode(gin.ReleaseMode) //gin框架中的 模式转发方法
	case "test":
		gin.SetMode(gin.TestMode)
	case "debug":
		fallthrough //fallthrough 只会强制跳到【紧邻的下一个 case】，不是最后一个，也不是跳过所有！
	default:
		gin.SetMode(gin.DebugMode)
	}

}

//gin.Mode() 可以获取当前设置好的 模式

最后在main.go中初始化的时候调用

